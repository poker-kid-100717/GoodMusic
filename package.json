{
  "name": "goodmusic-edge",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that serves the GoodMusic Next.js site and routes the API to its ASP.NET Core container.",
  "scripts": {
    "build": "npm run build:cloudflare --prefix web",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260925.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.2",
    "wrangler": "^4.140.0"
  }
}
