// The Next.js app compiled for Workers by OpenNext (`npm run build`). The file
// only exists after a build, so type-checking can't follow the import.
// @ts-ignore -- generated at build time
export { default } from "../web/.open-next/worker.js";
